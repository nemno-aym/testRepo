git_url("https://github.com/nemno-aym/testRepo.git")

type("enterprise") # The default type, can be: appstore, adhoc, enterprise or development

app_identifier("test.app.ci")
username("norbert@aymcommerce.com") # Your Apple Developer Portal username
clone_branch_directly(true)
# readonly(true)
# keychain_name(ENV["APPLE_CERTIFICATE_KEYCHAIN"])

# For all available options run `fastlane match --help`
# Remove the # in the beginning of the line to enable the other options
